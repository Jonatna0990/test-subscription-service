app:
  env: "local"
  name: "subs service"

http_server:
  host: "localhost"
  port: 3001

postgres:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "example"
  db_name: "postgres"
  ssl_mode: "disable"